development:
  domain: localhost:3000
  omni_auth_providers: [:tqq, :douban, :tsina, :github, :renren]

test:
  domain: test.host
  omni_auth_providers: [:tqq, :douban, :tsina, :github, :renren]

production:
  domain: example.com
  omni_auth_providers: [:tqq, :douban, :tsina, :github, :renren]
